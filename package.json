{
  "name": "conversation-to-markdown",
  "version": "1.7.0",
  "private": true,
  "description": "Copy a complete ChatGPT conversation as clean Markdown.",
  "type": "commonjs",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check content.js && node --check popup.js && node --check zip.js"
  }
}
